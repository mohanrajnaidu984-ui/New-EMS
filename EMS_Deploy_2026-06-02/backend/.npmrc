# Skip downloading Chromium browser during npm install because it often fails 
# in strict corporate firewall environments with "end of central directory record signature not found".
# The backend will automatically fall back to the pre-installed Google Chrome/Edge on the Windows Server.
puppeteer_skip_download = true
