# IIS / production frontend build — must match server PORT and proxy-server target.
VITE_API_PORT=5002

# Download PDF uses browser Print → Save as PDF (no Puppeteer on user click).
# Email / background jobs still use POST /api/quote-pdf/generate on the API server.
VITE_QUOTE_PDF_BROWSER_DOWNLOAD=1
