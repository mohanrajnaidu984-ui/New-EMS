# IIS / production frontend build — must match server PORT and proxy-server target.
VITE_API_PORT=5002

# Set to 0 to enable direct server-side PDF downloads (matching local behavior)
VITE_QUOTE_PDF_BROWSER_DOWNLOAD=0

